generic_icon = domain + '.svg'
symbolic_icon = domain + '-symbolic.svg'

# Install icons
install_data(generic_icon, install_dir: generic_icon_dir)
install_data(symbolic_icon, install_dir: symbolic_icon_dir)
